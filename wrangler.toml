name = "x402-ai"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# Статика: Cloudflare Pages берёт содержимое папки x402-site/ как корень
# Деплой: npx wrangler pages deploy . --project-name x402-ai
